/// A placeholder class that represents an entity or model.
class Feature {
  const Feature(this.id, this.name, this.routeName, this.imageUri);

  final int id;
  final String name;
  final String routeName;
  final String imageUri;
}
